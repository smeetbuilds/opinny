[build]
  command = "bun install && bun run build"
  publish = ".next"

[build.environment]
  BUN_VERSION = "1.3.14"
  NODE_VERSION = "22"
  NETLIFY_NEXT_SKEW_PROTECTION = "true"

[dev]
  command = "bun run dev"
  targetPort = 3000
  port = 8888
  autoLaunch = false
